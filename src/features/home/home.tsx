import { Button, Container } from "../../common";

export const Home: React.FC = () => {
  return (
    <Container>
      <h2>Home</h2>
      <Button content="Button" handleOnClick={() => {}} />
    </Container>
  );
};
